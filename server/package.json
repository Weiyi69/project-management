{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "prisma generate",
    "build": "echo 'No build step needed for server'"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0",
    "@neondatabase/serverless": "^0.10.2",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  }
}
